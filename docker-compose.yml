services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: airport_fuel
      POSTGRES_USER: airport_fuel
      POSTGRES_PASSWORD: airport_fuel
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

